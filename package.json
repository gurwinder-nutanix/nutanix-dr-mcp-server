{
  "name": "dr-mcp-server",
  "version": "1.0.0",
  "description": "A MCP server to interact with the DR V4 APIs. For now, this can be used to list the recovery points, VMs, etc.",
  "type": "module",
  "main": "dr-mcp-server.js",
  "scripts": {
    "start": "node dr-mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  }
}